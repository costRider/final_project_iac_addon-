server:
  service:
    type: ClusterIP

  extraArgs:
    - --insecure

  ingress:
    enabled: true
    ingressClassName: gce
    annotations:
      kubernetes.io/ingress.class: "gce"

    hosts:
      - argocd.136.110.154.85.nip.io   # <-- 여기만 바꿈

    paths:
      - /
    pathType: Prefix
